{
  "name": "earth-cinematic",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "check:copy": "node scripts/check-user-copy.mjs",
    "dev": "npm run check:copy && vite",
    "build": "npm run check:copy && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fontsource-variable/onest": "^5.3.0",
    "three": "^0.179.1"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
